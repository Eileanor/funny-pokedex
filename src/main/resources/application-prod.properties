# Production profile — Redis cache (shared across all replicas).
# CacheConfig picks up @Profile("prod") bean.
spring.data.redis.host=${REDIS_HOST:localhost}
spring.data.redis.port=${REDIS_PORT:6379}
spring.data.redis.timeout=${REDIS_TIMEOUT:30s}
spring.data.redis.client-type=lettuce